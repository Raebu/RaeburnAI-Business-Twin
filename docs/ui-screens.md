# UI screen capture notes

The dashboard route is `/`.

Capture before launch:

- main dashboard
- scenario panel
- workflow cost panel
- health response

Save images in `docs/assets/` when the final design is approved.
